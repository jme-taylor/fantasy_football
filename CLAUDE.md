## Verifying Changes

- Use the same commands found in `.github/workflows/ci.yml` for linting/formatting/type checking and testing. If you need to run one individually, take that single command. E.g. if you're asked to do type checks, only run the CI command for type checks.